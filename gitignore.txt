# --- Checkpoints and solver artefacts -------------------------------------
# Monte Carlo and carbon-price-sweep checkpoints are regenerated by running the
# notebook and are not committed.
*.pkl
mc_checkpoints/
tradeoff_checkpoints/
*.lp
*.sol

# --- Credentials and secrets ----------------------------------------------
# The Copernicus Climate Data Store API key must never be committed.
.cdsapirc
*.env
.env

# --- Input data -----------------------------------------------------------
# The two datasets required to reproduce the published results ARE committed:
#   era5_wind_100m_2023_8760h.nc
#   chemical_industry_load_profile_8760h.csv
# Any OTHER downloaded ERA5 files or user demand files are ignored below.
# (The committed files are tracked explicitly and are not matched by these rules.)
*.grib
era5_*_raw.nc
demand_*.csv

# --- Generated outputs ----------------------------------------------------
# Figures and tables are regenerated by the notebook. Committed publication
# figures, if any, live under figures/ and can be force-added.
*_full.csv
*_table.csv
tradeoff_*.csv

# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
env/
*.egg-info/

# --- OS / editor ----------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
