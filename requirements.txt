# Version-pinned environment for reproducibility of the published results.
# PyPSA 0.20.1 is frozen because later releases alter component cost attributes;
# the remaining bounds match the environment the results were produced in.
# The GLPK solver is invoked with pyomo=False. Python 3.12.
#
# System-level dependencies are installed via the host package manager, not pip:
#   glpk-utils            (the GLPK linear-programming solver)
#   libproj-dev libgeos-dev  (PROJ and GEOS, for the optional mapping figures)

pypsa==0.20.1
numpy<2.0
pandas<2.3
pyomo<6.7
scipy<1.12

# Solver interface used by PyPSA's native (pyomo=False) path.
linopy

# Plotting and geospatial (study-site maps; optional but imported by the map cells).
matplotlib
cartopy

# Wind resource acquisition from the Copernicus Climate Data Store (ERA5).
cdsapi
xarray
netCDF4

# Progress bars for the Monte Carlo and carbon-price sweep cells.
tqdm
